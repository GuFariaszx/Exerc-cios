// 9. Faça um programa que receba um número do usuário e mostre
// no console a contagem regressiva até 0.

let reg = prompt("Informe um número Inteiro qualquer: ")

while (reg >= 0) {
    console.log(reg)
    reg--   
}